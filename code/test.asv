
userLimit = var1;
userData = var2;
userNum = var3;
optimal = var4;
demand = optimal(2,:) * -1;
production = optimal(1,:);


y = [production;demand];
x = 1:userNum;
bar(x,y,'stacked');
xlabel('User Index')
ylabel('production/demand cost')
legend('Production','Demand');

T = 

netCost(p,d,alpha1,alpha2,beta1,beta2);
x = solveoptimization(T{i,'plow'}, T{i,'dlow'},T{i,'phigh'},T{i,'dhigh'},T{i,'alpha1'},T{i,'alpha2'},T{i,'beta1'},T{i,'beta2'}, a, userNum, newLambda);